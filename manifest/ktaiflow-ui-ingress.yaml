apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ktaiflow-ui-ingress
  namespace: ${NAMESPACE}
  annotations:
    networking.gke.io/managed-certificates: ${KTAIFLOW_UI_CERT_NAME}
    ingress.kubernetes.io/ssl-redirect: "true"
    ingress.gcp.kubernetes.io/force-cleanup: "true"
spec:
  ingressClassName: gce
  rules:
    - host: aiflowui.${DOMAIN}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: ktaiflow-ui
                port:
                  number: 80
